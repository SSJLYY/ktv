<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ktv.mapper.OrderSongMapper">

    <sql id="Base_Column_List">
        os.id,
        os.order_id,
        os.song_id,
        os.song_name,
        os.singer_name,
        os.sort_order,
        os.status,
        os.play_time,
        os.finish_time,
        os.create_time,
        os.update_time,
        os.deleted
    </sql>

    <!-- 查询订单下的点歌列表，可按状态过滤 -->
    <select id="selectByOrderIdAndStatus" resultType="com.ktv.entity.OrderSong">
        SELECT
            <include refid="Base_Column_List"/>,
            s.duration,
            s.file_path
        FROM t_order_song os
        LEFT JOIN t_song s ON os.song_id = s.id AND s.deleted = 0
        WHERE os.order_id = #{orderId}
          AND os.deleted = 0
        <if test="status != null">
            AND os.status = #{status}
        </if>
        ORDER BY os.sort_order ASC, os.create_time ASC
    </select>

    <!-- 查询已播列表，只返回已播放和已跳过的记录 -->
    <select id="selectPlayedByOrderId" resultType="com.ktv.entity.OrderSong">
        SELECT
            <include refid="Base_Column_List"/>,
            s.duration,
            s.file_path
        FROM t_order_song os
        LEFT JOIN t_song s ON os.song_id = s.id AND s.deleted = 0
        WHERE os.order_id = #{orderId}
          AND os.deleted = 0
          AND os.status IN (2, 3)
        ORDER BY os.finish_time DESC, os.play_time DESC
    </select>

    <select id="findSongInfoById" resultType="com.ktv.entity.OrderSong">
        SELECT
            <include refid="Base_Column_List"/>,
            s.duration,
            s.file_path
        FROM t_order_song os
        LEFT JOIN t_song s ON os.song_id = s.id AND s.deleted = 0
        WHERE os.id = #{orderSongId}
          AND os.deleted = 0
    </select>

</mapper>
