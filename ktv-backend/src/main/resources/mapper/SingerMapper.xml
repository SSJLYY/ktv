<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ktv.mapper.SingerMapper">

    <resultMap id="SingerVOMap" type="com.ktv.vo.SingerVO">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="pinyin" property="pinyin"/>
        <result column="pinyin_initial" property="pinyinInitial"/>
        <result column="gender" property="gender"/>
        <result column="region" property="region"/>
        <result column="avatar" property="avatar"/>
        <result column="song_count" property="songCount"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectPageWithConditions" resultMap="SingerVOMap">
        SELECT 
            id, name, pinyin, pinyin_initial, gender, region, avatar, 
            song_count, status, create_time, update_time
        FROM t_singer
        WHERE deleted = 0
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="region != null and region != ''">
            AND region = #{region}
        </if>
        ORDER BY id DESC
    </select>

</mapper>
